import { CombatWheelDialPipe } from './combat-wheel-dial.pipe';

describe('CombatWheelDialPipe', () => {
  it('create an instance', () => {
    const pipe = new CombatWheelDialPipe();
    expect(pipe).toBeTruthy();
  });
});
